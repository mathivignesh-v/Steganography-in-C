# Steganography-in-C
Steganography in C is a project used to hide secret data inside a BMP image without affecting its appearance. It uses the LSB (Least Significant Bit) technique for secure data embedding and is developed using C concepts like file handling, bitwise operations, structures, and command-line arguments.
